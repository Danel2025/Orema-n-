/**
 * Template de génération de ticket client ESC/POS
 * TODO: Implémenter les commandes ESC/POS
 */

import type { TicketData, EtablissementInfo } from "../types";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function generateTicketCommands(
  printer: any,
  etablissement: EtablissementInfo,
  data: TicketData,
  width: number
): void {
  // Stub - à implémenter
  // Les commandes seront ajoutées au printer
}
