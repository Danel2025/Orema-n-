/**
 * Template de génération de bon cuisine/bar ESC/POS
 * TODO: Implémenter les commandes ESC/POS
 */

import type { BonCuisineData } from "../types";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function generateBonCuisineCommands(
  printer: any,
  data: BonCuisineData,
  width: number,
  destination: "CUISINE" | "BAR"
): void {
  // Stub - à implémenter
  // Les commandes seront ajoutées au printer
}
